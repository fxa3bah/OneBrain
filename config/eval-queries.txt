Which machine is primary and which agents run on it?
What is the hostname of each machine on the local network?
Where does the Obsidian vault live and is it on a cloud-sync folder?
Which embedding model does gbrain use and is it local or cloud?
Which chat model is gbrain configured to use, and is it actually installed?
Where do operational secrets live and which ones are still unrotated?
Which email address should reports and notifications be sent to?
Which sender address is used for outbound agent email?
What is the current VPS or server host and what runs on it?
Which DNS or tunnel provider account owns each public hostname?
What is the canonical design system file and how is it propagated?
Which agents are wired to the gbrain MCP endpoint?
What is the agent isolation rule and what is excluded from it?
Which model tier is used for gathering versus reasoning?
Which model is allowed for raw paid-API calls?
What is the backup arrangement for the vault?
Which project or customer is the largest active engagement?
Where is the primary financial tracker and which tabs matter?

# Replace every line above with questions about YOUR world.
#
# The point is to probe facts that actually go stale: hostnames and IPs, which model is
# configured versus which is installed, where secrets live, which systems were retired.
# Those are what produce confidently wrong answers, and they are what the nightly
# `gbrain eval suspected-contradictions` run is looking for.
#
# Keep your filled-in version LOCAL. It describes your infrastructure. The installer
# copies this file to ~/.gbrain/eval-queries.txt — edit it there, not here.
